# Codex CLI

## AGENTS.md

Create a file `AGENTs.md` and add on `~/.codex/AGENTS.md` for global instructions.

## Custom Commands

Create a file `CUSTOM_COMMANDS.md` and add on `~/.codex/prompts/CMD.md` for global custom commands.